# Firebase Client Configuration (Test Environment)
# Get these from: https://console.firebase.google.com/project/finsight-ai-test/settings/general
NEXT_PUBLIC_FIREBASE_API_KEY=your_test_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_test_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_test_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_test_project.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# E2E Test Credentials
# Create a test user in Firebase Authentication for E2E tests
E2E_TEST_EMAIL=test@example.com
E2E_TEST_PASSWORD=your_secure_password_here

# Firebase Admin SDK (Server-side only)
# NEVER commit actual service account keys to the repository
# Store these in GitHub Secrets for CI/CD
# Get these from: Firebase Console → Project Settings → Service Accounts → Generate New Private Key
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_CLIENT_EMAIL=your_service_account@project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour_Private_Key_Here\n-----END PRIVATE KEY-----\n"

# Other Service Keys (NEVER commit actual keys)
PLAID_CLIENT_ID=your_plaid_client_id
PLAID_SECRET=your_plaid_secret
PLAID_ENV=sandbox
OPENAI_API_KEY=your_openai_api_key

# Test Environment Variables (Optional)
# By default, tests use mocked Plaid services (no credentials needed)
# To test real Plaid integration, add your test credentials to .env.test.local:
# PLAID_CLIENT_ID=your_test_client_id
# PLAID_SECRET=your_test_secret
# PLAID_ENV=sandbox
# Tests will automatically use real Plaid client when credentials are present

# FX Rate API (Optional - falls back to static rates if not configured)
# Get a free key from: https://www.exchangerate-api.com/
EXCHANGE_RATE_API_KEY=your_exchange_rate_api_key

# Cron Job Secret (Required for production cron jobs)
# Generate a secure random string: openssl rand -hex 32
CRON_SECRET=your_cron_secret_here

# Upstash Redis (Optional - for rate limiting)
# Get from: https://console.upstash.com/
UPSTASH_REDIS_REST_URL=your_upstash_redis_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token

# Backfill Script Configuration (Optional)
# Number of months to backfill historical snapshots (default: 3)
MONTHS_BACK=3
